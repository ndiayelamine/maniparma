import { ProgettiMediaModel } from './progetti-media-model';

describe('ProgettiMediaModel', () => {
  it('should create an instance', () => {
    expect(new ProgettiMediaModel()).toBeTruthy();
  });
});
